[unix_http_server]
file=/run/supervisor.sock   ; (the path to the socket file)

[supervisord]
logfile=/srv/log/supervisord.log ; (main log file;default $CWD/supervisord.log)
logfile_maxbytes=50MB        ; (max main logfile bytes b4 rotation;default 50MB)
logfile_backups=10           ; (num of main logfile rotation backups;default 10)
loglevel=info                ; (log level;default info; others: debug,warn,trace)
pidfile=/run/supervisord.pid ; (supervisord pidfile;default supervisord.pid)
nodaemon=false               ; (start in foreground if true;default false)
minfds=1024                  ; (min. avail startup file descriptors;default 1024)
minprocs=200                 ; (min. avail process descriptors;default 200)

; the below section must remain in the config file for RPC
; (supervisorctl/web interface) to work, additional interfaces may be
; added by defining them in separate rpcinterface: sections
[rpcinterface:supervisor]
supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface

[supervisorctl]
serverurl=unix:///run/supervisor.sock ; use a unix:// URL  for a unix socket

[program:nginx]
command=/usr/bin/nginx -c /srv/etc/nginx.conf

[program:mysql]
command=/usr/bin/pidproxy /run/mysqld/mysqld.pid /usr/bin/mysqld_safe
user=mysql

[program:php-fpm]
command=/usr/bin/php-fpm --nodaemonize

;http://www.industrialisation.it/dockerizing-cgit/
[program:fcgiwrap]
command=spawn-fcgi -n -u http -s /run/fcgiwrap.sock /usr/bin/fcgiwrap

[program:nbviewer]
command=/usr/bin/python2 -m nbviewer --port=8080 --no-cache --mathjax_url=/static/mathjax/
directory=/srv/http
user=shmilee
;redirect_stderr=true  ;tail nbviewer to debug
